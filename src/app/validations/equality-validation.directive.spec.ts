import { EqualityValidationDirective } from './equality-validation.directive';

describe('EqualityValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualityValidationDirective();
    expect(directive).toBeTruthy();
  });
});
