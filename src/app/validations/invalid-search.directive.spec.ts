import { InvalidSearchDirective } from './invalid-search.directive';

describe('InvalidSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidSearchDirective();
    expect(directive).toBeTruthy();
  });
});
