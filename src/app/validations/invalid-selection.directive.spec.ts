import { InvalidSelectionDirective } from './invalid-selection.directive';

describe('InvalidSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
